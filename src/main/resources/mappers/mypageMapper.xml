<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mypage">
	<select id="userinfo" resultType="userinfo">
		select id, password, name, nickname, to_char(birth,'yyyy-mm-dd') birth, email, tel, addr, profile, secure, secure_ans, to_char(regdate,'yyyy-mm-dd') regdate, to_char(updatedate,'yyyy-mm-dd hh24:mi:ss') updatedate
		from userinfos
		where id=#{id}
	</select>
	
	<delete id="delete">
		delete from userinfos
		where id=#{id}
	</delete>
	
	<update id="update">
		update userinfos 
		set pass=#{pass},nickname=#{nickname},email=#{email},secure=#{secure},secure_ans=#{secure_ans}
		where id=#{id}
	</update>
	
	<!-- 아이디를 받아 비밀번호를 비교하기위해... -->
	<select id="auth" resultType="int">
		select count(*) from userinfos where id like #{id} and password like #{pass}
	</select>
</mapper>